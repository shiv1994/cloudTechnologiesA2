class SalesPerson {
    constructor(fName, lName) {
      this.firstName = fName;
      this.lastName = lName;
    }
  }

class Theatre {
  constructor(number, movieShowing) {
      this.number = number;
      this.movieShowing = movieShowing;
  }
}

class Sale{
  constructor(numTickets, saleType) {
    this.numTickets = numTickets;
    this.saleType = saleType;
  }
}